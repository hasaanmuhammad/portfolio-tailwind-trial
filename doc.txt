###penting
kalau mau manggil tailwind pake cdn harus :
copy link source tailwind, lalu buat new file dengan nama tailwind.config.js (kosongan aja) otomatis sudah aktif 

*hal ini dilakukan kalau tidak mempersoalkan optimasi web 
# Series Tailwind
utility first -> hanya 1 hal saja dan efektif
component first -> css lama/atau vanilla css

bedanya dengan bootstrap
bootstrap -> ui framework
tailwind -> utility first frameworks

tailwind antara gabungan bootstrap dengan vanilla css cuman tailwind itu nulis sendiri di classnya tapi fungsi css nya sudah dibuatkan dengan tailwind. 

secara fungsi tailwind lebih efektif dan efisiien 
pre-requisite 
pahami html, css, bootstrap, css pre-processor, npm 

sass itu pre-processor 

separation of conccerns -> terbagi menjadi structure, presentation, dan behavior -> html, css, dan js (perilaku)
nama class -> disesuaikan untuk peruntukannya 

BEM -> BLOCKS, ELEMENTS, MODIFIER

perilakunya sama yaudah kita copy aja tidak semantic buatlah sebuah cmonent untuk cari nama component yang media card jadi ndak ada duplikasi. down into the dawn 